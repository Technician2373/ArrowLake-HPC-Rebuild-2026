# Compiler & Toolchain Setup

This document covers the full compiler stack required for Arrow Lake HPC development:

- GCC + G++ (multilib)
- Clang + LLVM
- SPIR-V tools
- Vulkan loader
- Mesa build dependencies
- oneAPI HPC toolkit installation
- Additional libraries required for kernel, Mesa, and Vulkan builds
